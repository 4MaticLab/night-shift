# 塔罗画风夜兆抽卡

- 状态：`completed`
- 优先级：P1
- 创建：2026-07-24
- 更新：2026-07-24
- 负责人：GitHub Copilot
- 分支：`feat/tarot-card-draw-92`
- 关联：GitHub Issue #92
- 推进模式：`auto`

## 动机

收藏页已经保存夜印、植物、明信片、城市回声和口袋小物，但缺少一种可主动进行、适合黑客松现场展示的轻量仪式。Issue #92 要求加入“塔罗牌画风抽卡”；最合适的位置是收藏页中的独立夜兆牌桌，让抽取成为不影响主线的每日文学回声，而不是奖励、数值或付费随机机制。

## 范围

- 定义一组原创“雾灯夜兆牌”，复用已审核美术并以 CSS 建立塔罗牌画框、编号、正逆位和牌背。
- 在收藏页增加独立“夜兆牌桌”分类与响应式抽卡交互。
- 每个案件按本地自然日只能确认一张牌；刷新、切换页面或重启浏览器不能重抽。
- 抽取结果本地持久化，并按案件隔离；提供清晰的下一次可抽取时间与历史记录。
- 补充确定性选牌、日期边界、持久化和界面行为测试，以及稳定功能文档。

## 非目标

- 不改变案件线索、收藏品、结局条件、睡眠评价、社团关系或角色事实。
- 不加入稀有度、货币、十连抽、保底、付费、链上铸造或概率展示。
- 不请求账号、后端、网络随机数或系统通知权限。
- 不修改首页、夜间结算、海报、硬件、合约或其他无关功能。
- 不生成新位图；只复用仓库中已审核的首案美术资产。

## 任务

- [x] 定义原创牌组、确定性每日抽取规则和本地记录契约。
- [x] 实现收藏页夜兆分类、翻牌仪式、正逆位解读和历史牌列。
- [x] 添加塔罗牌画风、响应式布局、减少动态效果和无障碍边界。
- [x] 编写功能、触发条件与产品护栏文档。
- [x] 完成单元测试、lint、构建、本地浏览器和移动端检查。

## 验收标准

- 收藏页分类导航出现“夜兆牌桌”，进入后可看到牌背、规则和抽取按钮。
- 首次抽取产生一张确定的原创牌与正／逆位解读；同一案件同一自然日不能重抽。
- 刷新页面后保留当日结果，切换案件时各自保存独立结果。
- 抽卡只留下叙事解读，不修改任何游戏进度或成果字段。
- 键盘、移动端和 `prefers-reduced-motion` 下均可完成抽取并阅读结果。
- 文档明确入口、触发、日期边界、持久化、重置和非奖励边界。

## 验证

- `npm test`：15 个测试文件、124 项测试通过。
- `npm run lint`：通过。
- `npm run build`：通过。
- `npm run docs:check`：92 个 Markdown 文件全部通过。
- Playwright Chromium 目标回归：每日抽取、刷新持久化与 game store 隔离 1 项通过。
- 本地浏览器检查：牌背空状态、逆位翻牌、解读、历史记录、刷新后禁用状态均正确。
- 390 × 844 移动端：只显示当前夜兆分类，页面无横向溢出，牌面与解读单列可读。
- 减少动态效果：动画时长被全局规则压缩到 `0.01ms`，抽取和结果仍可用。

## 决定记录

- 2026-07-24：放在收藏页独立分类，不塞入夜间结算，避免把睡眠成果改造成抽卡奖励。
- 2026-07-24：采用“每案件每本地自然日一张”的固定仪式；结果由案件、日期与本地种子确定，刷新不能重抽。
- 2026-07-24：所有牌只提供文学解读，不提供优势、稀有度或进度。
- 2026-07-24：采用独立 `night-shift-tarot-v1` 存档，不升级或写入 game store。
- 2026-07-24：九张牌复用已审核 Night Shift 资产，以原创编号、牌名和正逆位解读形成雾灯城牌组。

## 相关文档

- [[docs/art-direction]]
- [[docs/north-star]]
- [[docs/architecture]]
- [[docs/privacy-and-guardrails]]
