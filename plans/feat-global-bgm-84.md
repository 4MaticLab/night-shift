# 全局背景音乐控制

- 状态：`in_progress`
- 优先级：P1
- 创建：2026-07-24
- 更新：2026-07-24
- 负责人：GitHub Copilot
- 分支：`feat/global-bgm-84`
- 关联：GitHub Issue #84
- 推进模式：`auto`

## 动机

Night Shift 已有完整视觉与等待氛围，但没有贯穿站点的音乐层。Issue #84 要求用户进入网页时自动播放“C 小调夜曲”，并让右上角始终保留可关闭的小图标。音频文件将由用户后续提供，因此本次需要先完成可替换 MP3 契约、浏览器自动播放兼容、持久化静音与缺失资源降级。

## 范围

- 在根布局挂载全局 BGM 控制器，使案件库、序章、游戏页和海报页共享同一音频实例。
- 默认开启并在首屏立即尝试播放；受浏览器自动播放策略拦截时，在下一次非控制器用户交互后重试。
- 右上角始终显示一个可访问的小图标，用于静音／恢复，偏好保存在浏览器本地。
- 音乐循环播放、低音量渐入渐出；页面隐藏时暂停，恢复可见时继续。
- 为待上传 MP3 建立固定路径、替换说明和资源缺失的诚实降级。
- 补充单元／端到端测试与稳定文档。

## 非目标

- 不生成、下载或提交未经授权的音乐文件。
- 不伪装浏览器已经允许自动播放；策略阻止时明确等待首次交互。
- 不加入曲库、播放列表、进度条、歌词、上传后台、流媒体或音量设置面板。
- 不改变游戏存档、睡眠、案件、海报、硬件、钱包或其他无关功能。
- 不让音乐影响结算、奖励、线索或结局。

## 任务

- [ ] 实现全局音频生命周期、自动播放重试、循环、可见性和持久化静音。
- [ ] 实现始终可见的右上角控制图标及响应式／打印／加载幕适配。
- [ ] 建立 `/audio/c-minor-nocturne.mp3` 文件契约与缺失资源状态。
- [ ] 编写 BGM 入口、浏览器限制、替换和验收文档。
- [ ] 运行测试、lint、构建、本地浏览器和移动端验证。

## 验收标准

- 根布局只挂载一个 `<audio>`，跨站内路由切换不重新开始曲目。
- 新访客默认尝试播放；允许自动播放时立即开始，拦截时首次页面交互后开始。
- 右上角按钮始终可见、可键盘操作并正确表达播放、关闭、等待交互或文件待上传状态。
- 用户关闭后刷新仍保持关闭；重新开启后立即尝试播放。
- 页面隐藏暂停、恢复可见继续；曲目循环且音量保持克制。
- MP3 缺失不会报错遮挡页面；上传指定文件后无需改代码。
- 打印海报时不输出控制按钮。

## 验证

- `npm test`
- `npm run lint`
- `npm run build`
- `npm run docs:check`
- 浏览器检查首页自动播放尝试、首次交互恢复、关闭、刷新持久化与路由连续性
- 390 × 844、加载幕、游戏顶栏和打印媒体检查

## 决定记录

- 2026-07-24：音频挂在根布局而非游戏布局，保证全部网页共享一个实例。
- 2026-07-24：固定资源路径为 `/audio/c-minor-nocturne.mp3`；本 PR 不提交占位音乐，避免版权与误交付。
- 2026-07-24：遵循浏览器自动播放策略；先立即尝试，再等待首次可信用户交互重试，不通过静音欺骗策略。
- 2026-07-24：只持久化关闭偏好，不写入游戏 store。

## 相关文档

- [[docs/architecture]]
- [[docs/art-direction]]
- [[docs/privacy-and-guardrails]]
