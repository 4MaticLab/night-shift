# 0041 — 恢复主线后的集成回归

- 状态：`completed`
- 优先级：P0
- 创建：2026-07-24
- 更新：2026-07-24
- 负责人：Codex
- 依赖：[[plans/0037-interactive-cipher-dials]]、[[plans/0038-first-load-experience]]、[[plans/0039-default-case-home-art-pass]]、[[plans/0040-browser-locale-negotiation]]
- 推进模式：`auto`

## 动机

队友强制恢复远端主线并合入密码盘、准备物 UI 等功能后，本地三笔提交已成功重放，但全量浏览器回归发现准备物命名契约、英文覆盖和案板坐标持久化出现四处集成回归。同步只有在这些能力共同工作后才算完成。

## 范围

- 统一新版准备物在交接单、AI 最小请求与测试中的稳定名称。
- 补齐新版准备物的英文投影，消除首案英文路径中的可见汉字。
- 修复桌面案板拖动结束后坐标未写入存档的问题。
- 更新同步后的质量基线、计划编号和验证证据。

## 非目标

- 不回退队友新增的准备物视觉、密码盘或雨夜电台功能。
- 不改变准备物 ID、存档版本、案件真相或结局条件。
- 不推送远端、不部署、不修改已有玩家存档。

## 任务

- [x] 统一新版准备物名称与 AI 请求契约。
- [x] 补齐准备物英文覆盖并恢复完整英文路径。
- [x] 修复并覆盖案板拖动坐标持久化。
- [x] 完成单测、lint、构建、文档和全量浏览器验收。

## 验收标准

- 新版“黄铜提灯／提灯”在 UI、AI 最小请求与测试中一致，旧 ID `side-lamp` 保持不变。
- 英文首案的准备物标题、短名和描述无可见汉字。
- 桌面拖动证物图钉后坐标写入 `boardPositions`，刷新恢复且“恢复摆放”仍可清空。
- 远端新增功能与本地加载、首页、语言功能共同通过完整验证。

## 验证

- `npm test`：79/79 通过。
- `npm run lint`：通过。
- `npm run build`：Next.js 生产构建通过，含两条休息仪式 API 路由。
- `npm run build:sites`：vinext/Sites 生产构建通过，含两条休息仪式 API 路由。
- `npm run docs:check`：77 份 Markdown、全部双链通过。
- `npm run test:e2e`：31/31 通过，包含两案密码台、加载页、自动语言、AI 休息仪式、案板拖动、黑水溪与 390×844 移动端。
- 本机 `http://localhost:3000` 返回 200，并恢复为可继续试玩的开发服务。

## 决定记录

- 2026-07-24：远端强制恢复后，本地三笔提交以 rebase 重放，并将冲突计划顺延为 0038–0040。
- 2026-07-24：全量浏览器回归 27/31 通过，四项失败均属于可回滚的仓库内集成回归；提案由 `proposed` 批准为 `approved` 后进入施工。
- 2026-07-24：准备物沿用队友新版“黄铜提灯／提灯”文案和稳定 ID `side-lamp`，AI 最小请求、英文词典、演示文档与测试随之同步，不回退新版视觉。
- 2026-07-24：案板 store 持久化本身正常；密码台加入案板上方后，浏览器脚本拖动了视口外坐标。测试先把图钉滚入视口再拖动，继续验证真实坐标写入与刷新恢复。
- 2026-07-24：rebase 还覆盖了桌面交接页原有的安全标题宽度；恢复 `330px／40%` 上限后，1280×720 的文字、交接单和林渡肖像重新互不遮挡。
- 2026-07-24：79 个单测、31 条完整浏览器路径、lint、双生产构建和 77 份文档全部通过，计划关闭。

## 相关文档

- [[docs/architecture]]
- [[docs/quality-baseline]]
- [[docs/viewport-checklist]]
