# 0031 — 案件板完整右栏与协作 PR

- 状态：`in_progress`
- 优先级：P1
- 创建：2026-07-23
- 更新：2026-07-23
- 负责人：Codex
- 依赖：[[plans/0030-case-board-right-inference-panel]]
- 推进模式：`manual`

## 动机

合并远端沙盒与睡眠硬件功能后，案件板组件仍按左画布、右推理栏组织，但最终双栏 CSS 在冲突解决时丢失，旧的绝对定位重新生效。实际页面因此把右栏悬浮在画布上方，匹配区和档案被裁切，并出现需要横向拖动的滚动条。用户要求在独立协作分支修复并提交 PR。

## 范围

- 恢复桌面端左侧画布、右侧普通内容框的真实双栏。
- 保证右栏、匹配槽、档案和核心论断完整适配列宽，不产生内部横向滚动。
- 在窄屏和手机端改为自然单列，画布之后完整显示推理、档案与核心论断。
- 修正合并后重复的计划编号并同步稳定文档。
- 在独立分支验证、提交、推送并创建草稿 PR。

## 非目标

- 不修改证物内容、配对答案或核心论断规则。
- 不改动黑水溪沙盒、睡眠硬件桥接或其他案件页面。
- 不发布生产部署。

## 任务

- [x] 修复桌面真实双栏和右栏完整宽度。
- [x] 修复窄屏／手机布局与横向溢出。
- [x] 更新自动化、计划编号和稳定文档。
- [x] 运行完整测试与浏览器验证。
- [ ] 提交协作分支并创建草稿 PR。

## 验收标准

- 1440 × 900 下画布只占左列，右栏是同级普通框而非绝对定位浮层。
- 右栏 `scrollWidth` 不大于 `clientWidth`，A／B 线索、核对按钮、档案与核心论断均完整显示。
- 页面不出现案件板导致的横向滚动条。
- 390 × 844 下按画布、匹配、档案、核心论断自然排列，底部导航不遮挡最后内容。
- 既有拖动、匹配、分享和双案件主路径保持通过。

## 验证

- 桌面 1440 × 900、1280 × 720 与手机 390 × 844 浏览器核对。
- `npm test`
- `npm run lint`
- `npm run build`
- `npm run test:e2e`
- `npm run docs:check`
- `git diff --check`

验证结果（2026-07-23）：

- 1440 × 900、1280 × 720 与 390 × 844 实机浏览器截图通过；右栏无横向溢出。
- `npm test`：62/62 通过。
- `npm run build`：通过。
- `npm run test:e2e`：23/23 通过。
- `git diff --check`：通过。
- `npm run lint`：主线既有 `src/components/game/shared.tsx` 渲染期读取 ref 问题导致 4 个错误，本分支未修改该文件。
- `npm run docs:check`：主线既有 Windows 路径解析问题把 574 个仓库内双链误判为越界，本分支未修改检查脚本。

## 决定记录

- 2026-07-23：用户明确要求新建协作分支、修复右栏完整显示并提交 PR，本 `manual` 计划获批并进入 `in_progress`。
- 2026-07-23：根因确认为合并时丢失最终双栏 CSS，而非 React Flow 数据或组件层级错误。
- 2026-07-23：分支使用 `agent/case-board-right-panel`；PR 默认创建为草稿，生产部署不在本计划范围。
- 2026-07-23：采用 `minmax(0, 1fr) + clamp(...)` 网格约束桌面列宽，右栏仅纵向滚动；1000 px 以下恢复自然文档流。

## 相关文档

- [[docs/architecture]]
- [[docs/quality-baseline]]
- [[docs/viewport-checklist]]
- [[plans/0030-case-board-right-inference-panel]]
